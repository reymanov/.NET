<Window x:Class="Aplikacje.NET.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Aplikacje.NET"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:Person x:Key="person"/>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel DataContext="{Binding Source={StaticResource person} ,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock>Wpisz imię i nazwisko</TextBlock>
            <TextBox Text="{Binding Name, Mode=TwoWay}" x:Name="txt" />
            <Label Content="{Binding FirstName, Mode=TwoWay}"/>
            <Label Content="{Binding LastName,Mode=TwoWay}"/>

            <TextBlock>Wpisz datę urodzenia w formacie Dzień.Miesiąc.Rok</TextBlock>
            <TextBox Text="{Binding Date, Mode=TwoWay}"/>
            <Label Content="{Binding Years, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</Window>
